import React from 'react';
import { Typography } from '@material-ui/core';

const Home = props => {
    
    return (
        <Typography variant="h2">
            Home page
        </Typography>
    )
}

export default Home;